import { GamesComponent } from './games.component';

describe('GameComponent isolated tests', () => {
    let component;

    beforeEach(() => {
        component = new GamesComponent();
    });

    afterEach(() => {
        component = undefined;
    });

    it('should ', () => {
        // write test
    });
});
